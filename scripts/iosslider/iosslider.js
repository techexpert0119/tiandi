$(window).on('load',function(){
  // use instead of ready so that images have loaded first
	$('.iosSlider').iosSlider({
					desktopClickDrag: true
	});
	$('.iosSlider1').iosSlider({
					desktopClickDrag: true
	});
});
